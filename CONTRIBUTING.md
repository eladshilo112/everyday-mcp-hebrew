<div dir="rtl">

# תרומה למאגר

תודה שבחרתם לפתור בעיה אמיתית, לא לייצר עוד דוגמה שלא עוזרת לאף אחד.

## מהו פתרון יומי טוב

* פותר בעיה נפוצה ומוגדרת במשפט אחד
* שימושי לאדם ממוצע, לא לארגון או למשתמש מסוים
* עובד מקומית כברירת מחדל
* כולל שרת MCP תקני ב־stdio
* כולל פלט מובנה ופלט אנושי
* כולל בדיקות יחידה ובדיקת MCP מקצה לקצה
* מתועד בעברית פשוטה עם דוגמה שאפשר להעתיק

## מבנה חובה

```text
solutions/NNN-slug/
├── README.md
├── metadata.json
├── package.json
├── package-lock.json
├── tsconfig.json
├── src/
└── tests/
```

## שער קבלה

לפני Pull Request יש להריץ:

```bash
python scripts/validate_catalog.py
python -m unittest discover -s tests -v
python scripts/validate_solution.py solutions/NNN-slug --install
```

הבדיקה אוכפת רשימת תלויות מצומצמת, חוסמת סקריפטים מסוכנים של npm, מריצה TypeScript ובדיקות קבועות וסורקת דפוסים נפוצים של סודות.

## כללי תוכן

אין להוסיף מידע אישי, כתובות דוא"ל פרטיות, נתוני לקוחות, חומר עסקי, קוד קנייני, תוכן מכירות או שיווק, סודות או נתונים שהועתקו ממקור שאינו מאפשר זאת.

## תהליך יומי אוטומטי

האוטומציה רשאית להוסיף רק תיקיית פתרון חדשה ולעדכן את `CATALOG.md`. היא אינה רשאית לשנות Workflows, מדיניות אבטחה, סקריפטי אימות או פתרונות ישנים. כל שינוי עובר CI בענף נפרד לפני מיזוג.

## ביטול ושחזור

כל פתרון נכנס כ־Pull Request מבודד. לפני מיזוג אפשר לסגור את ה־PR. לאחר מיזוג אפשר לבצע `git revert` למיזוג. אין למחוק היסטוריה ואין לבצע force push ל־`main`.

</div>
